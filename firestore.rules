rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Todo el acceso a Firestore va por el backend (Admin SDK).
    // El frontend no accede directamente, por lo que se deniega todo acceso directo.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
