name: Keep Render Alive

on:
  schedule:
    - cron: '*/10 * * * *'  # cada 10 minutos

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend
        run: curl -s https://ccretaria.onrender.com/health
